# Professional dark theme, pinned so every visitor sees the same contrast
# regardless of their system preference. Swiss palette: light ink on near-black,
# single refined red accent.
[theme]
base = "dark"
primaryColor = "#FF453A"
backgroundColor = "#0B0B0D"
secondaryBackgroundColor = "#141418"
textColor = "#ECECEE"
font = "sans serif"
